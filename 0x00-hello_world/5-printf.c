#include <stdio.h>
/**
 * main - entry point
 * Description: prints a string
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	/* ruturn - returns 0 if there are no errors */
	return (0);
}
