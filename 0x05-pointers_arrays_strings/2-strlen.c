#include "main.h"
/**
 * _strlen - returns the length of a string
 * Return: char
 * @s: parameter passed to the function
 */
int _strlen(char *s)
{
	unsigned long int a;

	a = sizeof(s);
	return (a);
}
