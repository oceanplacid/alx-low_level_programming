#include <stdio.h>
#include "main.h"
/**
 * mul - multiplies the two parameters passed to mul
 * Return: returns the result of the multiplication
 * @a: first parameter 
 * @b: second parameter
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
